# 软件需求规格说明书 (SRS)：Laptop Sentinel MVP v1.0

## 1. 项目定位

Laptop Sentinel 是一个运行在个人笔记本上的本地哨兵系统。它使用摄像头进行低资源运动侦测，在发现明显画面变化时自动保存录像、缩略图和事件记录，并通过一个精致的桌面控制面板供用户布防、撤防、查看时间线和打开录像。

本项目不是企业级安防平台，也不是云端监控服务。MVP v1.0 的目标是做出一个个人可用、交互完整、界面精美、结构可靠的本地桌面应用。

## 2. MVP 范围

### 2.1 本版包含

- 摄像头采集与运动检测。
- 布防、撤防、触发、冷却等后台状态管理。
- 触发前内存预录制，默认开启。
- 触发后固定时长录像。
- 本地保存 MP4 录像、JPG 缩略图和事件索引。
- 独立桌面窗口控制面板。
- 精致的时间线 UI，支持查看事件、状态、缩略图和文件可用性。
- 点击事件后调用系统默认视频播放器打开录像。
- 设置页或设置面板，用于调整常用参数。
- 本地日志与基础异常恢复。

### 2.2 本版暂不包含

- 远程 Webhook 告警。
- 人脸识别、目标分类、身份识别。
- 云端同步或远程访问。
- 多摄像头同时监控。
- 后台系统服务安装器。

Webhook 告警作为 v1.1 迭代项保留设计空间，但不进入 MVP v1.0 的核心交付范围。

## 3. 架构要求

系统采用“前端造壳，后端铸魂”的三层架构。

### 3.1 前端表现层

前端使用 HTML、CSS 和 JavaScript 构建现代桌面应用界面。MVP 阶段优先使用原生 Web 技术和本地静态资源，不引入 Vue/Vite 等构建链，避免早期复杂度膨胀。

前端职责：

- 渲染应用主窗口。
- 展示当前布防状态、摄像头状态和最近事件。
- 展示历史事件时间线。
- 提供布防/撤防按钮。
- 提供设置入口。
- 调用 Python 暴露的桥接 API。

### 3.2 桥接与桌面外壳层

桌面外壳使用 `pywebview`。它负责创建独立桌面窗口，并将本地 HTML/CSS/JS 注入窗口。Windows 上依赖 Edge WebView2，macOS 上依赖系统 WebKit，Linux 上依赖 GTK/WebKit 运行时。

桥接层职责：

- 创建应用窗口。
- 暴露 Python API 给 JavaScript。
- 将 UI 操作转发给 Python 后端。
- 将后端状态变化通知前端。
- 调用系统默认视频播放器打开录像文件。

### 3.3 核心后端层

后端使用 Python、OpenCV 和 SQLite。摄像头采集、运动检测和录制逻辑必须运行在独立后台线程中，不能阻塞 UI。

后端职责：

- 管理摄像头生命周期。
- 执行帧差法、背景差分或等价轻量算法。
- 维护触发前环形缓冲区。
- 录制事件视频并生成缩略图。
- 写入事件数据库。
- 提供状态查询、事件查询、配置读写和播放接口。

## 4. 状态定义

- **撤防态 (Disarmed):** 不读取摄像头，不检测运动。应用窗口和后台服务仍可运行。
- **布防态 (Armed):** 持续读取摄像头，低帧率检测运动。默认不写磁盘，但在内存中维护预录制缓冲。
- **触发态 (Triggered):** 检测到运动后进入，系统保存预录制片段和触发后片段，生成事件证据。
- **冷却态 (Cooldown):** 单次事件完成后进入防抖周期，期间忽略新触发，避免重复事件。
- **查阅态 (Review):** 用户查看事件和设置的 UI 场景。查阅态不影响后台状态。

状态转换要求：

- 用户点击“布防”：`Disarmed -> Armed`。
- 运动命中阈值：`Armed -> Triggered`。
- 事件保存完成：`Triggered -> Cooldown`。
- 冷却结束：`Cooldown -> Armed`。
- 用户点击“撤防”：后台应尽快进入 `Disarmed`。若正在保存事件，必须先完成当前事件固化。
- 打开、关闭、刷新 UI 不得自动改变后台状态。

## 5. 功能需求

### 5.1 后台监控引擎

- 系统必须读取配置指定的摄像头设备。
- 摄像头不可用时必须记录错误并允许用户重试，不得导致应用崩溃。
- 运动检测应使用轻量算法，MVP 不使用云端模型。
- 必须支持灵敏度、最小运动面积、检测帧率和分辨率配置。
- 生产模式不显示摄像头预览窗口；调试预览必须由配置显式开启。
- 后台引擎必须能向 UI 提供当前状态、错误信息、最近事件时间和摄像头状态。

### 5.2 预录制与录像

- 默认开启触发前预录制。
- 预录制使用固定容量内存环形缓冲，默认 3 秒。
- 预录制不得持续写磁盘。
- 触发后录制默认 10 秒。
- 事件视频总时长约等于预录制秒数加触发后录制秒数。
- 若设备性能不足，用户可关闭预录制或降低预录制秒数、帧率和分辨率。

### 5.3 证据存储

- 每次触发必须生成一个唯一事件 ID。
- 每次触发必须保存录像文件和缩略图文件。
- 文件命名建议为 `EVENT_YYYYMMDD_HHMMSS.mp4` 和 `EVENT_YYYYMMDD_HHMMSS.jpg`。
- 写文件时应使用临时文件，完成后再原子替换或重命名，避免 UI 读取半成品。
- 事件索引必须使用 SQLite。MVP 不使用只追加 JSONL 作为主索引。

### 5.4 桌面控制面板

- 应用必须以独立桌面窗口运行，而不是要求用户手动打开浏览器。
- 控制面板应具备现代视觉设计，支持深色主界面或精致暗色工作台风格。
- 控制面板应支持主题切换，至少提供一套克制的默认主题和一套深色主题。
- 首页展示当前状态、布防/撤防操作、最近事件和事件统计。
- 首页应提供可手动开启的实时摄像头视窗。实时视窗必须复用后台捕捉引擎的画面数据，不得额外打开第二个摄像头实例。
- 实时视窗默认关闭；开启后渲染帧率上限可跟随 `capture_fps`。当用户离开总览页、窗口不可见或后续最小化到托盘时，应停止前端渲染。
- 时间线按时间倒序展示事件。
- 每条事件至少展示触发时间、事件标签、缩略图、录制时长和文件可用状态。
- 点击事件或播放按钮时，系统调用默认视频播放器打开录像。
- 文件丢失或损坏时，UI 应显示清晰提示，不影响其他事件展示。
- UI 刷新、筛选或查看事件不得重启或暂停后台监控。

### 5.5 设置管理

MVP 应提供配置文件，并尽量提供 UI 设置入口。配置至少包含：

- 摄像头编号。
- 存储目录。
- 采集帧率。
- 检测帧率。
- 画面宽高。
- 运动灵敏度。
- 最小运动区域面积。
- 触发后录制秒数。
- 是否开启预录制。
- 预录制秒数。
- 冷却秒数。
- 是否开启调试预览。

## 6. 非功能需求

### 6.1 可用性

- 应用应能通过一个清晰入口启动。
- 首次启动时，如果配置不存在，应自动生成默认配置。
- 关键操作必须有明确反馈，例如布防成功、撤防成功、摄像头不可用、事件保存完成。
- UI 不应出现明显浏览器感，不应要求用户理解 Web 服务端口。

### 6.2 性能

- 布防状态下检测帧率默认限制在 5-10 FPS。
- 预录制缓冲区必须固定容量，不得无限增长。
- 摄像头读取、图像处理和录像写入不得阻塞 UI。
- 普通笔记本上运行时不应造成明显卡顿。

### 6.3 鲁棒性

- 摄像头被占用或初始化失败时，应用仍应保持可操作。
- 录像写入失败时，系统应记录错误，并在 UI 中展示事件失败状态。
- 单条事件异常不得导致整个时间线不可用。
- 数据库损坏或读取失败时，应提供可诊断日志。

### 6.4 隐私与安全

- 系统仅用于用户本人授权的设备和环境。
- 程序不得绕过操作系统摄像头权限、系统摄像头指示灯或用户安全策略。
- MVP 默认不上传视频、缩略图或事件数据。
- 配置和日志不得泄露敏感路径之外的隐私信息。

## 7. 事件数据模型

SQLite 事件表建议字段：

- `id`: 主键。
- `event_id`: 事件唯一 ID。
- `triggered_at`: ISO 8601 时间，包含时区。
- `label`: 事件标签，默认“画面移动”。
- `video_path`: 录像路径。
- `thumbnail_path`: 缩略图路径。
- `pre_record_seconds`: 预录制秒数。
- `post_record_seconds`: 触发后录制秒数。
- `duration_seconds`: 实际视频时长。
- `motion_score`: 触发时运动评分或运动面积。
- `status`: `saved`、`partial`、`failed`。
- `error`: 可选错误信息。
- `created_at`: 记录创建时间。

## 8. MVP 验收标准

- 应用以独立桌面窗口启动，不要求用户打开浏览器。
- 用户可以在 UI 中布防和撤防。
- 布防后摄像头开始低资源运动检测。
- 画面出现明显运动后，系统保存一段包含预录制画面的事件视频。
- 系统为事件生成缩略图和 SQLite 记录。
- 时间线可以展示历史事件。
- 点击事件可以调用系统默认播放器打开录像。
- 打开、关闭、刷新 UI 不影响后台监控状态。
- 摄像头不可用、文件丢失、录像失败时，UI 能给出可理解提示。
- 默认配置即可运行，常用参数可调整。
